import React from 'react';
import ActivityTooltip from './ActivityTooltip';

const StatefulActivityTooltip = (props) => {
    return (
        <ActivityTooltip {...props} />
    );
};

export default StatefulActivityTooltip;